---
ldap:
  src:
    uri: {{ RSC_SRAM_SYNCHRONIZER_SOURCE_URI }}
    basedn: dc={{ RSC_SRAM_SYNCHRONIZER_SOURCE_BASEDN }}
    binddn: cn={{ RSC_SRAM_SYNCHRONIZER_SOURCE_CN }},dc={{ RSC_SRAM_SYNCHRONIZER_SOURCE_BASEDN }}
    passwd: {{ RSC_SRAM_SYNCHRONIZER_SOURCE_BIND_PWD }}
  dst:
    uri: {{ RSC_SRAM_SYNCHRONIZER_DEST_URI }}
    basedn: dc={{ RSC_SRAM_SYNCHRONIZER_DEST_BASEDN }}
    binddn: cn={{ RSC_SRAM_SYNCHRONIZER_DEST_CN }},dc={{ RSC_SRAM_SYNCHRONIZER_DEST_BASEDN }}
    passwd: {{ RSC_SRAM_SYNCHRONIZER_DEST_BIND_PWD }}
pwd: changethispassword
uid: 10000
gid: 10000
